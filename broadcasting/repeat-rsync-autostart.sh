#!/bin/sh
#  This script runs the rsync command over an over to sync up the pictures
# add this script to the ~/.config/lxsession/Lubuntu/autostart file

while true ;

do syncfilesscript.sh & sleep 15 ;

done
